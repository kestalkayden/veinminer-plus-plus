accessWidener	v2	official

# Reopen RenderPipelines.register (private) so the shape guide can register its custom
# depth-disabled lines pipeline. Sodium/Iris silently drop UNregistered custom pipelines
# (they never enter the pipeline set those mods scan), so the guide vanishes in-pack while
# rendering fine in vanilla. See common/.../client/ShapeGuideRenderer.java.
accessible	method	net/minecraft/client/renderer/RenderPipelines	register	(Lcom/mojang/blaze3d/pipeline/RenderPipeline;)Lcom/mojang/blaze3d/pipeline/RenderPipeline;
