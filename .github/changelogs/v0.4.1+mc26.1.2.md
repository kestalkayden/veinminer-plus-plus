## Changed
- Tree felling now stops scanning a shared leaf canopy as soon as it reaches a neighbouring tree, instead of walking the whole canopy before leaving it to decay — a small performance improvement when vein-felling in dense forest. No change to what gets broken or dropped.
